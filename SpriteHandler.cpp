#include "SpriteHandler.h"

/*
template<class T>
void SpriteHandler<T>::spriteModifier(U& object,sf::Sprite& sprite, sf::RenderWindow& window){
	sprite.setPosition(gameObject.getX(),gameObject.getY());
	sprite.setScale(gameObject.getEnemyRadius()/1000, gameObject.getEnemyRadius()/1000);
	lookAt(sprite,gameObject.getEnemyAngle());
	gameObject.colliderUpdate(sprite.getGlobalBounds());
}*/
